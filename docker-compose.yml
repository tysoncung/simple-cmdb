version: '3.8'

services:
  cmdb:
    build: .
    container_name: simple-cmdb
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./cmdb.db:/app/cmdb.db
    environment:
      - FLASK_ENV=production
      - DOCKER_CONTAINER=1
    restart: unless-stopped